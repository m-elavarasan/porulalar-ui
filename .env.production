# Production Environment Configuration
# If frontend & backend are served behind the same domain/Nginx, leave empty to use relative /api
# If backend is on a dedicated domain, specify the full URL: e.g. VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
