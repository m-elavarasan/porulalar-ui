import { googleService } from '../services/googleService';

export const createCalendarReminder = googleService.createCalendarReminder;
export const fetchGmailTransactions = googleService.fetchGmailTransactions;
